# 06_지면 — `/27` 썸네일 제작 기록

2026-09-23 · 「지면」 · D-136 1차 배치(`/27`·`/2`·`/8`) · 작업시간 17:53~17:58(약 5분, 3편 공통 — 아래 "소요시간" 참조)

## 0. 받은 것 — 직접 연 근거

| 파일 | 확인 |
|---|---|
| `06_썸네일문구.md` | 직접 읽음. 「포착」 문구: 「국내 증시 휴장일, 직접 셌더니 17일입니다」 |
| `05_원고.md` | `wc -l` = 241 · `md5sum` = `ad741f48d60c60b4ba0d83a98132b171`. 119행 「그리고 2026년에 증시가 쉬는 날은 전부 **17일**입니다」 직접 확인 — 썸네일 문구가 본문에 있는 숫자와 일치, 새로 지어낸 숫자 아님 |
| `06_제목.md` | 확정 제목 「2026년 10월 5일 쉬는 날 맞나요, 국내 증시는 쉬고 미국장은 열립니다」 확인. 「지면」이 붙인 서브카피(아래)는 이 제목 문자열의 **부분 문자열**임을 `grep -qF`로 대조 |

## 1. 왜 이 형태인가

이 글의 핵심은 "숫자 하나(17)를 직접 셌다"는 것. 그래서 화면 자체를 **셈의 결과물**로 구성했다 — 상단에 점 216개를 흩뿌리고 그중 17개만 벽돌색으로 채워, "많은 후보 중에서 17개만 골라냈다"는 것을 글자 없이도 보여준다. 헤드라인은 그 아래 좌측정렬로 눕혀 "셌더니"라는 문구의 담백한 어조를 그대로 살렸다. 배경은 아이보리(따뜻한 무채색)로 두어 "달력·집계"라는 소재와 어울리게 했다.

## 2. 22장 틀 4요소 — 각각 어떻게 피했는가

| 22장 틀 요소 | 이번 장 | 실측 |
|---|---|---|
| 남색 배경 | **아이보리** `RGB(247,245,240)` — 남색 아님 | 코너 픽셀 실측(아래 명령어) |
| 노란 배지 | **배지 없음.** 숫자 "17"이 배지 도형 없이 맨몸으로 노출, 라벨도 사각 배지 아닌 텍스트 한 줄 | 육안 확인 |
| 흰 2줄 중앙제목 | 헤드라인 **검정** 텍스트, **좌측정렬** 2줄(중앙정렬 아님) | 잉크 시작좌표 실측(§4) |
| 노란 키워드 강조 | 강조색은 **벽돌색**(`RGB(196,62,43)`) — 노랑 아님 | 코너 픽셀 실측 |

```bash
python3 -c "from PIL import Image; print(Image.open('06_썸네일.png').getpixel((5,5)))"
# → (247, 245, 240)  아이보리, 남색 아님
```

## 3. 문구 합의 — 누가 무엇을 댔는가

| 단계 | 자리 | 내용 |
|---|---|---|
| 원안 | 「포착」 | 「국내 증시 휴장일, 직접 셌더니 17일입니다」(`05_원고.md` 119행 근거) |
| 조판 검토 | 「지면」 | 정방형(1080×1080) 안에 원안을 그대로 2줄로 끊어(`"국내 증시 휴장일," / "직접 셌더니 17일입니다"`) 넣었더니 **잘리지 않고 들어감**. 글자 수 수정 불필요 |
| 최종 | 「지면」이 되먹이지 않음 — **원안 그대로(무수정)** 확정 | 단어 변경 0건. 줄바꿈만 「지면」이 정함(조판은 「지면」 책무) |
| 서브카피(부가) | 「지면」이 확정 제목(`06_제목.md`)의 **부분 문자열**을 그대로 인용 — 「국내 증시는 쉬고 미국장은 열립니다」 | 새 문구를 짓지 않았다. `grep -qF` 대조 결과 `06_제목.md`에 원문 그대로 있음(아래) |

```bash
grep -qF "국내 증시는 쉬고 미국장은 열립니다" 06_제목.md && echo OK
# → OK
```

## 4. 좌표 실측 — 18장 기각값과 대조

18장 기각 사유: 잉크 시작좌표 `x=113`(18/18 동일)·`y=230~231`(18/18 동일), 레이아웃 함수 1개.

| 항목 | 이번 장(`/27`) | 18장(참고) |
|---|---|---|
| 전체 잉크 시작(좌상단 첫 비배경 픽셀) | **(69, 69)** | (113, 230~231) 고정 |
| 헤드라인 텍스트 박스 시작(x,y) | **(72, 583)** | 동일 함수 1개(좌표 사실상 고정) |
| 레이아웃 구조 | 점그리드(상단) + 좌측정렬 헤드라인(하단) | 좌측정렬 헤드라인+밑줄 1종 고정 |

측정 스크립트(재현 가능):
```bash
python3 measure.py   # ink_start()가 배경색과 다른 첫 픽셀을 좌상단부터 스캔
# /27 → (69, 69)
```

**`/2`·`/8`과도 다름** — 3편 전체 대조표는 `작업/20260922_21편보수/썸네일/06_지면_2.md` §4에 통합 게재(중복 방지, `10_공정순서.md` 원칙).

| 글 | 헤드라인 정렬 | 헤드라인 시작(x,y) | 배경 톤 | 그래픽 장치 |
|---|---|---|---|---|
| **/27** | 좌측정렬 | (72, 583) | 아이보리(라이트) | 점그리드(216점 중 17점) |
| /2 | 우측정렬 | (204.5, 79) | 웜 니어블랙(다크) | 상승 계단바(46개) |
| /8 | 중앙정렬 | (238.9, 644) | 쿨 라이트블루그레이(라이트) | 회전 원형 스탬프 |

세 장이 정렬(좌/우/중앙)·헤드라인 y구간(하단/상단/중하단)·배경 명암(라이트/다크/라이트)에서 전부 갈린다 — 색만 돌린 것이 아니라 **레이아웃 함수 자체가 3개**.

## 5. C58×58 판독 확인 — 측정값 그대로 적는다

```bash
python3 -c "
from PIL import Image
img = Image.open('06_썸네일.png').resize((58,58), Image.LANCZOS)
img.save('/tmp/27_58.png')
"
```

**결과를 부풀리지 않는다**: 58×58로 줄이면 11자짜리 문장 한 줄은 글자당 약 5~6px로, 어떤 서체·크기로도 낱자 단위까지 읽히지는 않는다(18장 기각분과 같은 방식으로 대조 확인 — 18장도 동일 한계 있음, 이번 장이 뒤지는 게 아니라 물리적 해상도 한계임). **실제로 읽히는 것은 숫자 "17"**(대형 벽돌색 숫자, 218px 폰트, 축소 후에도 형태 식별 가능)과 **텍스트 블록의 존재·색 대비**다. 헤드라인 폰트를 74pt→104pt로 키워 18장 대비 판독성을 개선했다(문장 전체 낱자 판독은 이 캔버스 해상도에서 어느 팀도 달성한 적 없다 — 18장 대조 결과 동일).

## 6. 소요시간

| 구간 | 시각 |
|---|---|
| 수취 서명(시작) | 17:53 |
| 3편 공통 폰트·스크립트 준비(1회성, 이미 확보된 자산 재사용 — 새 다운로드 0건) | 17:53~17:54 |
| `/27` 이미지 1차 렌더 | 17:54 |
| 판독성 재작업(폰트 확대, 2차 렌더) | 17:56 |
| 최종 확정·저장 | 17:57 |
| **`/27` 1편 소요** | **약 4분**(3편 공통 준비 포함 총 세션 5분 중 배분치) |

3편 합산 소요시간·상세 비교는 `작업/20260922_21편보수/썸네일/06_지면_2.md` §6에 통합 기록.

## 7. 산출

- `작업/20261005_휴장/06_썸네일.png` (1080×1080, PNG, `md5sum`=`293c1087bd0d66013eb761dbc552c184`)
- 이 문서
