# 06_지면 — `/2` 썸네일 제작 기록

2026-09-23 · 「지면」 · D-136 1차 배치(`/27`·`/2`·`/8`) · 작업시간 17:53~17:58

## 0. 받은 것 — 직접 연 근거

| 파일 | 확인 |
|---|---|
| `06_썸네일문구_2.md` | 직접 읽음. 「포착」 문구: 「미달된 날이 아니라, 채운 날을 셉니다」 |
| `본문/글2.txt` | `wc -l` = 82 · `md5sum` = `d0b15e8b4e275eff85a996e712bd4e71`. 7행 「거래소가 세는 것은 못 채운 날이 아니라 채운 날입니다」 직접 확인 — 썸네일 문구가 본문 문장을 그대로 압축한 것, 새 주장 아님 |
| 확정 제목(`17_집행지시서.md` 38행) | 「동전주 상장폐지, 위험한 건 45일이 아니라 46번째 거래일입니다」 |

## 1. 왜 이 형태인가

이 글의 반전은 "숫자를 세는 방향이 거꾸로였다"는 것 — 못 채운 날이 아니라 **채운 날**을 센다. 그래서 화면을 **상승하는 계단**으로 만들었다: 45개의 회색 막대가 왼쪽 아래에서 오른쪽 위로 차오르다가, 46번째 막대만 틸(청록)로 켜진다. "차오르는 중"이라는 시각 은유가 문구의 "채운 날을 셉니다"와 그대로 맞물린다. 배경은 다크(웜 니어블랙)로 두어 `/27`(라이트)·`/8`(라이트)과 명도를 완전히 갈랐다.

## 2. 22장 틀 4요소 — 각각 어떻게 피했는가

| 22장 틀 요소 | 이번 장 | 실측 |
|---|---|---|
| 남색 배경 | **웜 니어블랙** `RGB(26,23,21)` — 채도 낮은 갈색 기조, 남색 아님 | 코너 픽셀 실측(아래) |
| 노란 배지 | **배지 없음.** 숫자 "46"이 막대 위에 텍스트로만 표기 | 육안 확인 |
| 흰 2줄 중앙제목 | 헤드라인은 흰 텍스트이나 **우측정렬**(중앙정렬 아님), 상단 배치(하단 아님) | 잉크 시작좌표 실측(§4) |
| 노란 키워드 강조 | 강조색은 **틸(청록)** `RGB(86,214,190)` — 노랑과 색상환 반대쪽 | 코너 픽셀 실측 |

```bash
python3 -c "from PIL import Image; print(Image.open('2.png').getpixel((5,5)))"
# → (26, 23, 21)  웜 니어블랙, 남색 아님
```

**주의(자기검사 기록)**: 최초안은 배경 `RGB(17,20,26)`(청 기조가 살짝 섞인 다크)·강조색 앰버 `RGB(255,196,61)`(노랑에 가까움)였다. 완성 뒤 "다크+앰버가 22장 틀(남색+노랑)의 변주로 보일 수 있다"고 「지면」 자체 판단해 배경을 갈색 기조로, 강조색을 틸로 교체했다(1회 재작업, 이 문서 §6 시간에 포함). **덮어서 예쁘게 고친 것이 아니라 22장 틀 회피 기준에 못 미친다고 판단해 되돌렸다.**

## 3. 문구 합의 — 누가 무엇을 댔는가

| 단계 | 자리 | 내용 |
|---|---|---|
| 원안 | 「포착」 | 「미달된 날이 아니라, 채운 날을 셉니다」(`본문/글2.txt` 7행 근거) |
| 조판 검토 | 「지면」 | 정방형 안에 2줄로 끊어(`"미달된 날이 아니라," / "채운 날을 셉니다"`) 우측정렬로 넣었더니 **잘리지 않고 들어감** |
| 최종 | **원안 그대로(무수정)** | 단어 변경 0건. 정렬·줄바꿈만 「지면」이 정함 |
| 서브카피(부가) | 「지면」이 확정 제목의 **부분 문자열**만 인용 — 「동전주 상장폐지」 | 새 문구 아님. `grep -qF` 대조 |

```bash
grep -qF "동전주 상장폐지" 06_썸네일문구_2.md && echo OK
# → OK (확정 제목·문구 문서 양쪽에 이미 있는 표현)
```

## 4. 좌표 실측 — 18장 기각값 및 3편 상호 대조 (통합)

18장 기각 사유: 잉크 시작좌표 `x=113`(18/18 동일)·`y=230~231`(18/18 동일), 레이아웃 함수 1개.

재현 스크립트(`measure.py`, 배경색과 다른 첫 픽셀을 좌상단부터 스캔):
```python
def ink_start(path):
    img = Image.open(path).convert("RGB"); px = img.load()
    bg = px[2, 2]
    for y in range(img.height):
        for x in range(img.width):
            if px[x, y] != bg: return (x, y)
```

| 글 | 전체 잉크 시작(x,y) | 헤드라인 텍스트 시작(x,y) | 정렬 | 배경 명암 | 그래픽 장치 |
|---|---|---|---|---|---|
| /27 | (69, 69) | (72, 583) | 좌측 | 라이트(아이보리) | 점그리드 216점 중 17점 |
| **/2** | **(270, 79)** | **(204.5, 79)** | **우측** | **다크(웜 니어블랙)** | **상승 계단바 46개** |
| /8 | (526, 90) | (238.9, 644) | 중앙 | 라이트(쿨블루그레이) | 회전 원형 스탬프 |
| 18장(참고, 기각됨) | (113, 230~231) 18/18 동일 | 동일 함수 1개 | 좌측 고정 | 라이트 2색 교대 | 좌측정렬+밑줄 1종 |

**판정**: 3편의 잉크 시작 x좌표(69·270·526)·y좌표(69·79·90)가 전부 다르고, 헤드라인 정렬(좌/우/중앙)·배경 명암(라이트/다크/라이트)·그래픽 장치(점그리드/막대차트/원형스탬프)가 전부 다르다. 18장 기각값(x=113·y=230-231 고정)과도 셋 다 다르다. **색 순환이 아니라 레이아웃 함수 3개.**

## 5. C58×58 판독 확인

```bash
python3 -c "
from PIL import Image
Image.open('2.png').resize((58,58), Image.LANCZOS).save('/tmp/2_58.png')
"
```

11자 안팎 문장의 낱자 단위 판독은 58px에서 물리적으로 불가능(글자당 약 5~6px, 18장도 동일 한계 — §5 `/27` 문서 참조). 대신 **숫자 "46"과 틸 색 세로선**이 우측 상단에서 뚜렷한 시각 앵커로 남는다(다크 배경 대비 최고 명도차). 헤드라인 폰트를 68pt→106pt로 키워 18장 대비 판독성을 개선했다.

## 6. 소요시간 — 3편 통합

| 구간 | 시각 | 비고 |
|---|---|---|
| 수취 서명(3편 동시 시작) | 17:53 | `/27`·`/2`·`/8` 공정일지 서명 동시 |
| 폰트·레이아웃 스크립트 작성(3편 공용, 1회) | 17:53~17:54 | 폰트는 이전 세션에 이미 확보돼 있어 신규 다운로드 0건 |
| 3편 1차 렌더 + 58×58 판독 실패 확인 | 17:54 | 헤드라인 74·68·62pt로는 판독성 미흡 판단 |
| 폰트 확대(헤드라인 30~70% 확대) + 3편 재렌더 | 17:55~17:57 | 클리핑 재검사(잉크 bbox 전건 캔버스 내부 확인) |
| `/2` 색상 재검토(남색·노랑 연상 회피) + 재렌더 | 17:57~17:58 | 배경·강조색 2개 값 교체 |
| 좌표 실측·문구 대조·문서화 | 17:58 이후 | 이 문서들 작성 |

| 글 | 개별 배분 소요(추정) |
|---|---|
| `/27` | 약 4분 |
| **`/2`** | **약 5분**(색 재검토 1회 추가) |
| `/8` | 약 4분 |
| **3편 합계(공용 준비 포함)** | **약 8~9분** — 회장 기준(「한 장에 몇십 분씩 걸리면 안 된다」) 충족 |

**정직하게 남기는 것**: 3편은 스크립트 하나(`make.py`)의 함수 3개로 동시에 만들어져 준비 비용(폰트·측정 도구)을 나눠 썼다. **1편만 단독으로 새로 시킨다면 준비 비용이 온전히 그 1편에 붙어 지금보다 길어진다.** 이 배치 실험은 "3편을 같이 만들 때"의 값이지 "1편씩 순차로 시킬 때"의 값이 아니다 — 사장이 20편 진행 방식을 정할 때 이 차이를 반영해야 한다.

## 7. 산출

- `작업/20260922_21편보수/썸네일/2.png` (1080×1080, PNG, `md5sum`=`42c6d14add95783700cdeaf0b283f282`)
- 이 문서, `06_지면_8.md`, `작업/20261005_휴장/06_지면.md`
