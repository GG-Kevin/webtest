# 썸네일 배경 RGB 누적표 (기록용 — 판정 기준 아님)

> 🔴 **D-140으로 「채널차 합 30」 규칙은 폐기됐다.** 이 표는 **참고 기록**으로만 남는다.
> **배경색이 전편과 가깝다는 것은 반려 사유가 아니다.** 반려는 **오류가 있을 때**와 **딱 봤을 때 진짜 별로일 때** 둘뿐이다(D-140).
> 「비슷하다」는 **똑같은 틀에 텍스트만 바꾼 것**을 말한다 — 겹치면 안 되는 것은 **레이아웃**이다.

## 값 (누적 — 매 장 추가)

| 글 | 배경 RGB | 파일 | 측정 시각/근거 |
|---|---|---|---|
| `/27` | `(247, 245, 240)` | `작업/20261005_휴장/06_썸네일.png` | D-139 (사장 실측) |
| `/8` | `(237, 241, 245)` | `작업/20260922_21편보수/썸네일/8.png` | D-139 (사장 실측) |
| `/28` | `(20, 20, 20)` | (D-136 대상, 파일 경로 미확인) | D-139 (사장 실측) |
| `/2` 반려본(1차) | `(26, 23, 21)` | `폐기/썸네일_20260923_D137반려_2/2_반려본.png` | D-139 (사장 실측) |
| `/2` 반려본(2차) | `(247, 242, 231)` | (폐기 — 2차 반려 시점 버전, 덮어써짐) | D-139 (사장 실측) — `/27`과 채널차 12, `/8`과 채널차 25. **반려** |
| `/2` 확정(3차) | `(230, 214, 178)` | `작업/20260922_21편보수/썸네일/2.png` | 「지면」 실측(`(5,5)` 픽셀) — `/27`과 채널차 **110**, `/8`과 채널차 **101**, `/28`과 채널차 **562**. 셋 다 30↑ 충족 |

## 대조표 — 채널차 합 (신규안 후보 vs 기존 전편)

새 배경 RGB를 여기 임시로 넣고 아래 셋 전부 30 이상인지 확인한 뒤에만 확정한다.

| vs | `/27 (247,245,240)` | `/8 (237,241,245)` | `/28 (20,20,20)` |
|---|---|---|---|
| 채널차 합 계산식 | `|R-247|+|G-245|+|B-240|` | `|R-237|+|G-241|+|B-245|` | `|R-20|+|G-20|+|B-20|` |

**주의**: `/28`은 니어블랙이라 라이트 배경이면 자동으로 충족되는 경우가 많다. **실제로 걸리는 것은 `/27`·`/8` 두 라이트 배경끼리다.**

## 2026-09-24 「지면」 재제작 2차 — 배경 변경 1건

| 장 | 이전 | 이후 | 사유 |
|---|---|---|---|
| `/6` | (26,24,22) 웜 니어블랙 | **(20,42,35) 딥 포레스트** | `/5` 버건디(56,18,26)와 명도·색상이 너무 가까웠다. 변경 후 R 채널차 36 ≥ 30(D-139 §2) 통과. G가 최대 채널이라 남색 편향 없음 |
| `/2` | (230,214,178) | 무수정 | D-139 검증값 유지 |
| `/8` | (237,240,245) | 무수정 | 원형 링 장식만 폐기, 배경색은 유지 |
