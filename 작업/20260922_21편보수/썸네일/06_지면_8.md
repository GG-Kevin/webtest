# 06_지면 — `/8` 썸네일 제작 기록

2026-09-23 · 「지면」 · D-136 1차 배치(`/27`·`/2`·`/8`) · 작업시간 17:53~17:58

## 0. 받은 것 — 직접 연 근거

| 파일 | 확인 |
|---|---|
| `06_썸네일문구_8.md` | 직접 읽음. 「포착」 문구: 「다음 반기신청은 2027년 3월입니다」 |
| `본문/글8.txt` | `wc -l` = 101 · `md5sum` = `37477d03fb2cc3d4a91adb214a69cd81`. 2행 핵심요약 박스 「하반기 소득분 반기신청은 **2027년 3월**」 직접 확인 — 문구가 본문 날짜를 그대로 옮긴 것, 새 날짜 아님 |
| 확정 제목(`17_집행지시서.md` 42행) | 「근로장려금 반기신청 마감, 지금은 정기신청만 남았습니다」 |

## 1. 왜 이 형태인가

이 글이 독자에게 주는 답은 **날짜 하나**(2027년 3월)다. 그래서 화면 중심에 그 날짜를 대형 스탬프처럼 회전시켜 찍었다 — "도장 찍힌 확정 정보"라는 인상을 주기 위함이다. 원형 테두리는 도장의 은유를 강화하되 장식이 아니라 스탬프 텍스트를 감싸는 구조적 요소로 썼다. 헤드라인은 그 아래 중앙정렬로 눕혀 스탬프와 대칭을 이룬다 — `/27`(좌측정렬)·`/2`(우측정렬)와 정렬 방향이 전부 다르다. 스탬프·캡슐 색(파랑 `#2b6cb0`)은 임의로 고른 게 아니라 **본문 `글8.txt` 2행 핵심요약 박스의 실제 테두리색**(`border-left: 4px solid #2b6cb0`)을 그대로 가져왔다 — 본문에 있는 색을 재사용했을 뿐 새로 지어낸 것이 아니다.

## 2. 22장 틀 4요소 — 각각 어떻게 피했는가

| 22장 틀 요소 | 이번 장 | 실측 |
|---|---|---|
| 남색 배경 | **쿨 라이트블루그레이** `RGB(237,241,245)` — 밝은 배경, 남색(다크) 아님 | 코너 픽셀 실측(아래) |
| 노란 배지 | **배지 없음.** 원형 스탬프는 사각 배지가 아니고 채움도 없다(테두리 원 + 텍스트) | 육안 확인 |
| 흰 2줄 중앙제목 | 헤드라인은 **흰색 아닌 진남색 텍스트**, 배경이 밝아 "흰 글자"가 아예 성립하지 않는 구조 | 코너·헤드라인 색 실측 |
| 노란 키워드 강조 | 강조색은 **파랑**(본문 인용, §1 근거) — 노랑 아님 | 코너 픽셀 실측 |

```bash
python3 -c "from PIL import Image; print(Image.open('8.png').getpixel((5,5)))"
# → (237, 241, 245)  라이트블루그레이, 남색 아님
```

## 3. 문구 합의 — 누가 무엇을 댔는가

| 단계 | 자리 | 내용 |
|---|---|---|
| 원안 | 「포착」 | 「다음 반기신청은 2027년 3월입니다」(`본문/글8.txt` 2행 근거) |
| 조판 검토 | 「지면」 | 정방형 안에 2줄로 끊어(`"다음 반기신청은" / "2027년 3월입니다"`) 중앙정렬로 넣었더니 **잘리지 않고 들어감** |
| 최종 | **원안 그대로(무수정)** | 단어 변경 0건 |
| 서브카피(부가) | 「지면」이 확정 제목의 **부분 문자열**만 인용 — 「지금은 정기신청만 남았습니다」 | 새 문구 아님. `grep -qF` 대조 |

```bash
grep -qF "지금은 정기신청만 남았습니다" 06_썸네일문구_8.md && echo OK
# → OK
```

## 4. 좌표 실측 — 18장 기각값 및 3편 상호 대조

(측정 방법·통합표는 `06_지면_2.md` §4 참조 — 같은 스크립트 `measure.py`로 3편 동시 측정, 중복 게재 대신 이 절엔 `/8` 개별값만 남긴다)

| 항목 | `/8` |
|---|---|
| 전체 잉크 시작(x,y) | **(526, 90)** |
| 헤드라인 텍스트 시작(x,y) | **(238.9, 644)** |
| 정렬 | 중앙 |
| 배경 명암 | 라이트(쿨블루그레이) |
| 그래픽 장치 | 회전 원형 스탬프 |

18장 기각값(x=113·y=230-231 고정)과 다르고, `/27`(69,69 / 72,583 / 좌측)·`/2`(270,79 / 204.5,79 / 우측)와도 x·y·정렬 전부 다르다.

## 5. C58×58 판독 확인

```bash
python3 -c "
from PIL import Image
Image.open('8.png').resize((58,58), Image.LANCZOS).save('/tmp/8_58.png')
"
```

문장 전체의 낱자 판독은 58px에서 물리적 한계로 불가능(§`06_지면_2.md` §5 동일 근거). 대신 **회전된 큰 숫자 "2027.03"**(132pt, 대각선 배치라 축소돼도 사선 형태가 남아 다른 두 장의 수평 텍스트와 구분됨)과 **원형 테두리**가 시각 앵커로 남는다. 헤드라인 폰트를 62pt→96pt로 키워 18장 대비 판독성을 개선했다.

## 6. 소요시간

3편 통합 시간표는 `06_지면_2.md` §6에 있다. `/8` 개별 배분: **약 4분**(재작업 없이 1차 확대 렌더에서 확정).

## 7. 산출

- `작업/20260922_21편보수/썸네일/8.png` (1080×1080, PNG, `md5sum`=`65feabc838b6351c4ea01e1ab8da9d01`)
- 이 문서, `06_지면_2.md`, `작업/20261005_휴장/06_지면.md`
