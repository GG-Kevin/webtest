# 묶음4 — 확장·공통군 `/5` `/18` `/19` `/20` `/24` `/25`

**처분안 절단본**: `작업/20260922_21편보수/처분안_절단/04_묶음4.md` (13,399 B · §1 해당 6행 + §2 ㉢㉣ + §4 1·5·7·8단계 + §3 공통)
**이 묶음에는 통합이 없다. URL이 사라지지 않는다.** **6편으로 가장 많다.**

## D-085 2번 — 사장이 D-004를 **번복**했다

> **`/5`↔`/23`을 통합하지 않는다. 둘 다 확장한다.**
> D-004의 전제는 *"제도 설명과 실제 사례가 한 글에 있어야 완결된다"*였고, **실측으로 그 전제가 깨졌다** — 본문 겹침 21편 전 쌍 **상위 25위 밖** · `/23` 반복블록 비중 **21편 중 최저 24%** · 본문 표 **41행(최다)**.

**`/5`는 확장이다. `/23`과 합치지 마라.** `/23`은 묶음2 소관이니 건드리지 않는다.

## 🔴 `/10` — **판정만 내고 처분은 집행하지 않는다** (D-085 3번)

사장이 **별도 호출을 쓰지 않기로** 정했다. 이 묶음이 `/10` 라이브 전문을 **함께 받아** `/5`와의 중복을 **판정만** 한다.

- **`/10`의 본문 파일을 만들지 마라. 한 글자도 고치지 마라**
- 판정은 **별도 항목**으로 낸다 — `작업/20260922_21편보수/14b_10번_중복판정.md`
- 적을 것: **겹치는 대목 · 겹치는 분량(자수, 센 명령어 병기) · 어느 쪽이 그 내용의 제자리인가 · 근거 한 줄**
- **처분(무엇을 지울지)은 적되 집행하지 않는다.** 사장이 정한다
- 사장이 댄 이유: *"판정과 집행을 같은 손에 두면 **「자기가 쓰고 자기가 검토」**다"*

> `/10`은 9/21에 **전면 재작성**된 편이다. 라이브에 `본 글은 2026년 9월 기준 일반 정보이며 특정 종목의 매매를 권유하지 않습니다.`가 있고 정리본에는 없다.

## 확장 — **무엇이 비었는지가 실측으로 나와 있다**

| 편 | 처분안이 적은 빈 곳 |
|---|---|
| `/5` 관리종목 지정 효과 | **블록 걷으면 2,000자대**로 내려간다. 「지정되면 계좌에서 무엇이 바뀌나」(신용·대용증권·반대매매)가 고유분이다 |
| `/18` 연금저축 세액공제 | 표 32행(21편 중 2위)로 **재료는 있다.** **출처 언급 1회 · 내부링크 1개** |
| `/19` 해외주식 양도소득세 | 출처 14회로 상위인데 **본문 표 0행** — 세율·공제 계산이 전부 줄글이다 |
| `/20` 국민연금 조기 vs 연기 | 손익분기점·건보 피부양자 각도는 고유. **표 6행 · 출처 1회 — 근거가 얇다** |
| `/24` 환율과 내 지갑 | 표 25행. **출처 1회 — 숫자가 예시뿐이다** |
| `/25` 유상증자 공시 | 표 8행 · 출처 1회. **`/10`이 공시 1,124건으로 한 것을 여기서는 안 했다** |

> **줄이는 것이 목적이 아니다.** `/6`처럼 걷으면 1,854자만 남는 편은 **반드시 채워 넣어야 한다**(§3).
> **채울 재료는 1차 출처에서 새로 가져온다.** 국세청·국민연금공단·DART·KRX·국가법령정보센터·한국은행.
> **직접 센 것을 넣어라.** 「몇 건 중 몇 건」이 이 사이트가 가진 유일한 고유 재료다(`/23`의 36개 실명 분류표, `/10`의 공시 1,124건이 그 예다).

## 내부 링크 — **21편 중 4편에만 있다**

Essentials가 *"링크를 크롤링 가능하도록 설정하여 Google에서 페이지에 있는 링크를 통해 사이트의 다른 페이지를 찾을 수 있도록 합니다"*라고 쓴다.
**문장 안에 녹인 문맥 링크로 건다.** 목록·말미 모음은 안 된다(`11_점검표.md` 11번 3항).
`/9`·`/14`·`/15`는 **사라질 URL이니 걸지 마라**(묶음1 소관).

## 이 묶음이 할 일

1. **§3 「전부 제거」 문자열을 뗀다**
2. **목차·FAQ·요약박스를 편마다 다시 판단한다** — **6편이 같은 답을 내면 그 자체가 §3 ⓒ 위반이다**
3. **얇은 편을 채운다** — 1차 출처 · 직접 센 것
4. **`/10` 중복 판정만 별도 항목으로** (위)

## 내는 것

`작업/20260922_21편보수/본문/글5.txt` `글18.txt` `글19.txt` `글20.txt` `글24.txt` `글25.txt`
`작업/20260922_21편보수/14_묶음4_기록.md` — 편 단위
`작업/20260922_21편보수/14b_10번_중복판정.md` — **판정만**
