# 묶음2 — 절 주고받기군 `/6` `/7` `/11` `/12` `/23`

**처분안 절단본**: `작업/20260922_21편보수/처분안_절단/02_묶음2.md` (14,831 B · §1 해당 5행 + §2 ㉢㉣ + §4 1·4·5단계 + §3 공통)
**이 묶음에는 통합이 없다. URL이 사라지지 않는다.** 하는 일은 **절을 주고받는 것**이다.

## 🔴 묶음1이 먼저 돌았다 — **두 파일을 먼저 열어라**

| 파일 | 무엇 |
|---|---|
| `작업/20260922_21편보수/이관_1_병합감자_29→7.md` | 묶음1이 `/2` §5 · `/9` §5에서 **잘라낸 병합·감자 절 원문**. **`/7`이 받는다** |
| `작업/20260922_21편보수/이관_2_시총표_23→2.md` | 묶음1이 `/2`에 **시가총액 상향 일정표를 착지시킨 위치**(절 번호 + 소제목 문자열) + **`/23`이 그 자리에 대신 걸 문맥 링크 문장** |

**둘 다 [수취]에 적어라.** 없거나 비어 있으면 **「진행」에게 되돌린다. 대신 지어내지 마라.**

## 이 묶음이 할 일 — 셋

### ① `/23` §4 시가총액 상향 일정표를 **뗀다** (D-085 2-부)
사장이 D-004(`/5`↔`/23` 통합)를 **번복**했다. **`/23`은 살아남고 확장 대상이다.**
> *"진짜 중복은 `/5`가 아니라 **`/2` §6·`/9`와 세 번 반복되는 시가총액 상향 일정표**다"*
> *"통합만 취소하면 중복은 남고 통합만 사라진다"* — **그래서 이 이관은 통합 취소와 같이 집행한다.**

- `/23` §4에서 그 표를 **떼고**, 그 자리에 `이관_2_...md`가 준 **문맥 링크 문장**을 넣는다
- ⚠ **`/23`의 36개 종목 실명 분류표(표 41행)는 건드리지 마라.** 처분안이 *"21편 중 고유성이 가장 높다 · 이 사이트에만 있는 고유 콘텐츠"*로 적은 것이고, 사장이 D-085에서 *"고유 표 41행을 가진 URL을 없애는 것은 역행"*이라고 썼다
- **`/2`는 고치지 않는다.** 묶음1 소관이다

### ② `/7`이 병합·감자 절을 **받는다**
§2 ㉣: *"겹치는 것은 `/7`의 §4 한 절뿐이다. 통합하면 이미 9,039자인 필라가 더 커진다. **방향을 뒤집어 `/2`·`/9`의 그 절을 잘라 `/7`로 보낸다**."*
- `이관_1_...md` 원문을 `/7`에 녹인다. **덧붙이지 말고 `/7` §4와 합쳐 한 절로 만든다.** 같은 말을 두 번 하면 이관한 의미가 없다
- **`/9`는 사라질 URL이다**(묶음1이 `/2`로 통합). **`/9`로 가는 링크를 새로 만들지 마라**

### ③ 매매정지·VI 비교표를 **한 편에만 둔다**
§4 4단계 · §2 ㉣ 첫 행. `/6`(정리매매) · `/11`(VI) · `/12`(단기과열) **셋이 서로를 한 절씩 또 설명한다** — `/11` §5·§6, `/12` §6.

- **「VI·사이드카·서킷브레이커·거래정지·단기과열·정리매매」 비교표는 한 편에만 두고 나머지는 문맥 링크로 보낸다.** 처분안: *"이게 ⓒ에 닿는 부분이다"*
- **어느 편에 둘지는 이 호출이 정한다.** 정하고 **근거 한 줄**을 기록에 남긴다
- **셋의 고유 수치는 각자 자리에 남긴다** — VI 정적 10%·동적 3%/6%·2분 단일가 / 단기과열 3거래일 30분 단일가 / 정리매매 7거래일·가격제한폭 없음. **셋 다 고유 수치가 있어서 통합하지 않기로 한 것이다**

## 얇은 편 둘 — `/6`과 `/11`

`/6`은 **21편 중 가장 짧고(2,974자) 반복 블록 비중이 가장 높다(38%)**. **블록을 걷으면 1,854자만 남는다.**
`/11`도 유형표상 재료보강 대상이다(`01_유형분류.tsv` → `/6`=C,E · `/11`=C,E).

> **걷기만 하고 끝내지 마라.** §3: *"`/6`(2,974자)처럼 블록을 걷으면 1,854자만 남는 편은 **반드시 채워 넣어야 한다**."*
> 채울 재료는 **1차 출처**에서 — 한국거래소 상장규정·매매제도 안내, KIND. **직접 센 것을 넣어라.**

## 내는 것

`작업/20260922_21편보수/본문/글6.txt` `글7.txt` `글11.txt` `글12.txt` `글23.txt`
`작업/20260922_21편보수/12_묶음2_기록.md` — 편 단위 + **비교표를 어느 편에 뒀는지와 근거 한 줄** + **이관 2건이 실제로 붙었는지 확인한 줄**


---

## 🔴 「진행」이 전수로 센 것 — **죽은 내부 링크를 이 묶음에서 닫는다** (0콜)

라이브에 **글자 그대로 `https://moneyproducer.co.kr/N`** 인 링크가 **19건 / 10편** 있다. 템플릿 자리표시자 `N`이 치환되지 않고 발행됐다. **전건 HTTP 404다**(「진행」이 직접 두드렸다).

**이 묶음 담당분**: `/11` 2건 · `/12` 1건 (`/6`·`/7`·`/23`은 0건)

**센 명령어**: `grep -o 'moneyproducer\.co\.kr/N"' 폐기/20260922_실측_라이브/p<N>.html | wc -l`

**그 자리가 원래 가리키려던 글로 고치거나, 가리킬 곳이 없으면 링크를 걷어낸다.** §3이 인용한 Essentials가 *"링크를 크롤링 가능하도록 설정하여 …사이트의 다른 페이지를 찾을 수 있도록"*이라고 쓴다. **닫은 건수를 기록에 적어라.**
