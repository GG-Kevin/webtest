# 11 · 묶음1 작업 기록 — 통합군 `/2` `/9` `/13` `/14` `/15`

```
[수취] 받은 것: 기동프롬프트/공통_머리.md · 기동프롬프트/묶음1_꼬리.md ·
       처분안_절단/01_묶음1.md · 02_선행_실측.md · 발행/면책_페이지.html ·
       운영/작법표본/01_작법기준.md · 폐기/20260922_실측_라이브/p{2,7,9,13,14,15,23}.html
[수취] 이 재료로 일할 수 있는가: 예 — D-085가 남길 URL 둘(/2·/13)을 이미 확정했고,
       §2 ㉠㉡가 흡수할 고유분을 절 단위로 지목하며, 착지점(면책 페이지)이 이미 서 있다.
[수취] 못 받은 것: 설계서(7)·사전검(7b). 분량·구조 지정이 없어 집필자가 정하고 근거를 아래 남긴다.
[재발] grep '서사' 운영/12_재발목록.md → 0건. 「전 자리」 2줄(숫자 재확인·행번호 전파)을 걸어 두고
       ① 처분안·실측 자수를 옮겨 적지 않고 전건 다시 셌다 ② 이관 착지를 소제목 문자열로 지정했다.
```

**모든 자수는 이 한 명령으로 셌다.** 다른 명령으로 센 값은 이 문서에 없다.
```
python3 -c "import re,html,sys;b=open(sys.argv[1],encoding='utf-8').read();b=re.sub(r'<(script|style)\b.*?</\1>','',b,flags=re.S|re.I);print(len(re.sub(r'\s','',html.unescape(re.sub(r'<[^>]+>','',b)))))"
```

---

## 한 장 요약

| | 결과 |
|---|---|
| **5편 → 2편** | `/2`(←`/9`) · `/13`(←`/14`+`/15`) |
| **301 대상** | **3건** (아래 표) |
| **자수** | `/2` 6,766 → **7,423** (+9.7%) · `/13` 2,909 → **4,936** (+69.7%) |
| **§3 네 문자열** | 두 편 모두 **전건 0회** |
| **목차·FAQ·요약박스** | 두 편 모두 **전건 0개** |
| **내부 링크** | `/2` 5개(`/5` `/7` `/10` `/13` `/23`) · `/13` 3개(`/2` `/10` `/23`). **죽을 URL로 가는 링크 0개** |
| **깨진 링크 `/N`** | `/13` **3건 → 0건** |

---

## 🔴 301 대상 3건 — 사라질 URL → 남을 URL

| # | 사라질 URL | 제목(라이브) | → 남을 URL | 3개월 노출·클릭 | 사장 근거(D-085) |
|:-:|---|---|---|---|---|
| 1 | `https://moneyproducer.co.kr/9` | 코스닥 시가총액 기준 | **`/2`** | 13 · 0 | `/2` §6이 `/9`의 핵심 표와 2027-01 일정을 이미 싣는다 |
| 2 | `https://moneyproducer.co.kr/14` | 완전자본잠식 | **`/13`** | 80 · 2 | 301이면 에퀴티는 넘어간다. **제목이 곧 주제인 URL을 남긴다** |
| 3 | `https://moneyproducer.co.kr/15` | 공시벌점 10점 | **`/13`** | 29 · 2 | 〃 |

**301을 걸기 전에 함께 고쳐야 하는 것 — 이 셋을 가리키는 내부 링크**

| 가리키던 편 | 가리키던 곳 | 이번에 처리 |
|---|---|---|
| `/2` | `/9` 1개 · `/14` 1개 · `/15` 1개 | **묶음1이 닫았다** (아래 `/2` 항목) |
| `/13` | 깨진 `/N` 3개(원래 `/9`·`/14`·`/15` 자리) | **묶음1이 닫았다** |
| `/14` · `/15` | `/2` 각 1개 + 깨진 `/N` 1·2개 | **URL 자체가 사라지므로 처리 불요** |
| **그 밖의 편** | — | **센 결과 0건.** 명령: `grep -l 'moneyproducer\.co\.kr/\(9\|14\|15\)"' 폐기/20260922_실측_라이브/p*.html` → `p2.html` 하나뿐 |

> **`/26`은 이미 404다**(실측). **이번 3건은 404로 두지 않는다.**

---

## /2 — 필라. `/9` 흡수 + `/23` §4 받음 + 병합금지 조항 `/7`로 보냄

- **뗀 것 ①** 금융위 (1)(2) 병합·감자 금지 원문 blockquote + 해설 2문단 + 앞 문장 1개, **4블록 312자**. → `이관_1_병합감자_29→7.md`
  `python3 … 폐기/20260922_묶음1/cut2_병합금지.html` → **312**
- **뗀 것 ②** 안내 앞 문장 `본 글은 2026년 9월 기준 일반 정보이며 특정 종목의 매매를 권유하지 않습니다.` **1회 40자.** 면책 고지 페이지가 받는다. **대체 문장을 새로 만들지 않았다**(D-085 5번).
  `grep -c '일반 정보이며 특정 종목의 매매를 권유하지 않습니다' 본문/글2.txt` → **0**
- **뗀 것 ③** 사라질 URL로 가는 링크 **3개**(`/9` 1 · `/14` 1 · `/15` 1).
- **옮긴 것** `/9` §1·§2·§3·§5 → `/2` 새 절 1개 / `/23` §4 표 2행 → 같은 절 / 병합금지 조항 → `/7`(이관_1)
- **더한 것** 새 절 「**1,000원은 넘겼는데 우려 공시가 떴다면 — 옆줄이 하나 더 있습니다**」 **약 900자**.
  1차 출처는 **전부 흡수원의 것**이고 이번에 새로 연 1차는 **없다**. 흡수한 값: 시총 기준표(코스피 300→500억 · 코스닥 200→300억 / 2026.7.1 · 2027.1.1) · 25→5거래일 우려 공시 구조 · 「병합해도 시가총액은 불변」.
  **`/2`에 없던 값이라는 것을 센 명령어**: `grep -o '500억원' 폐기/20260922_실측_라이브/p2.html | wc -l` → **0** (같은 명령 `p9` → 1 · `p23` → 1)
- **라이브 자수 → 새 자수** **6,766 → 7,423자** (+657, **+9.7%**)
- **판단이 들어간 곳** — 다섯

  **ⓐ `/9`의 「고유분 둘」을 새 절 하나로 묶었다.** §2 ㉠은 ⓐ「5거래일 전 우려 공시」와 ⓑ「액면병합」 둘을 지목했는데, **ⓐ는 `/2`에 이미 동전주판 실물 공시로 들어 있다**(`'26.08.31부터 5매매거래일 지속될 경우`). 절을 둘 만들면 같은 구조를 두 번 설명하게 된다. **한 절 안에서 「이 구조가 옆줄에도 똑같이 돈다」로 붙였다.**

  **ⓑ `/9` §4·§6·FAQ는 안 옮겼다.** §4(판정구조는 동전주와 동일)는 `/2` 1·2절이 실물 공시로 이미 말한다. §6(KIND에서 확인하는 법)은 `/2`가 *"KIND는 제 쪽에서 403이 돌아왔습니다"*라고 적은 글이라 **같은 글 안에서 자기와 싸운다.** FAQ 3문항 중 「동전주+시총 동시 해당」은 `/9` 스스로 *"일반적인 추정"*이라 적었다 — **추정을 1차 없는 채로 옮기지 않는다.**

  **ⓒ 병합금지 조항을 `/2`에서 떼면서 「감자 10:1」 독자 질문의 답은 남겼다.** 그 질문은 `/2` 3절의 인용이고, 답을 통째로 보내면 **독자를 갈라놓고 답이 없는 자리**가 된다(작법기준 §14-3). 조항 원문은 `/7`로 보내고, **답 한 문장 + `/7` 문맥 링크**만 남겼다. 312자 → 157자.

  **ⓓ 「최근 1년 기산점 미확인」 한 줄을 `/2`에 안 남기고 `/7`로 딸려 보냈다.** 그 「모른다」는 조항에 붙은 것이지 `/2`의 논지에 붙은 것이 아니다. **조항이 가면 그 경계선도 같이 가야 한다**(작법기준 §13). 이관_1 §2-②에 「빼지 마라」로 박아 뒀다.

  **ⓔ `/14`·`/15` 링크 둘을 `/13` 하나로 합쳤다.** 앵커를 *"그 둘은 숫자가 아니라 **사람이 모여 앉아 판단합니다**"*로 잡았다 — **`/13`의 주장과 같은 말**이고, 글자를 늘리지 않았다(작법기준 §13 보강3).

---

## /13 — `/14` + `/15` 흡수

- **뗀 것 ①** §3 네 문자열 **4종 4회**: 종결문 1 · 안내 앞 문장 1 · 매수·매도 문장 1 · 하위글 문장 1. **합 143자.**
  `for s in …; do grep -c "$s" 본문/글13.txt; done` → **전건 0**
- **뗀 것 ②** **목차 블록**(8항 · 약 180자) — 제거.
- **뗀 것 ③** **핵심요약 박스** 3개(`/13`·`/14`·`/15`) — **박스 자리를 없앴다.** 내용은 도입과 본문으로 갈라 넣었다.
- **뗀 것 ④** **FAQ 블록 3개(총 9문항)** — **자리는 없애고 내용은 9문항 전부 본문 절로 편입.** 어느 문항이 어디로 갔는지:

  | 출처 | 문항 | 편입한 절 |
  |---|---|---|
  | `/13` | 형식적 요건도 개선기간을 받나 | 「대신 시간이 절반으로 줄었습니다」 마지막 문단 |
  | `/13` | 두 요건에 동시에 해당되면 | 「숫자가 재는 줄과 사람이 …」 4대 요건 표 아래 |
  | `/13` | 일반 투자자가 이의신청을 할 수 있나 | 「심사는 한 번에 끝나지 않습니다」 마지막 문단 |
  | `/14` | 자본잠식 vs 완전자본잠식 | 자본잠식률 계산 문단(50% / 100%) |
  | `/14` | 반기 통과하면 연말은 면제되나 | 완전자본잠식 절 마지막 문단 |
  | `/14` | 자본금·자본총계를 어디서 보나 | 계산식 바로 뒤 문단 |
  | `/15` | 벌점은 시간이 지나면 사라지나 | 벌점 절 마지막 문단(공시 확인법과 한 문단) |
  | `/15` | 벌점 10점이면 무조건 상폐인가 | **1절로 끌어올렸다** — 이 글의 「어?」와 같은 말이라 |
  | `/15` | 1일 매매정지와 실질심사 둘 다 걸리나 | 두 기준선 박스 바로 뒤 문단 |

- **뗀 것 ⑤** 깨진 링크 `href=".../N"` **3건** → 0건.
  `grep -o 'moneyproducer\.co\.kr/N"' 폐기/20260922_실측_라이브/p13.html | wc -l` → **3** / 같은 명령 `본문/글13.txt` → **0**
- **옮긴 것** `/14` 전 6절 + FAQ 3 → `/13` 절 「완전자본잠식 — 같은 말인데 6월에 걸리느냐 12월에 걸리느냐로 줄이 갈립니다」 / `/15` 전 6절 + FAQ 3 → 절 「벌점 10점은 여러 번 어겨야 쌓이는 게 아닙니다」
- **더한 것** 1차 출처를 **새로 열지 않았다.** 더한 것은 **연결 문장과 절 제목뿐**이고, 사실은 전부 세 편의 라이브 본문에서 왔다. `/2`·`/23`·`/10` 문맥 링크 3개가 새로 생겼다(이전 `/13`의 살아 있는 링크는 `/2` 2개뿐이었다).
- **라이브 자수 → 새 자수** **`/13` 2,909 → 4,936자.** 흡수원 합계 기준으로는 **2,909+2,271+2,476 = 7,656 → 4,936** (−2,720). 줄어든 것은 **목차 3 · 요약박스 3 · FAQ 틀 3 · 중복 설명**이고, **내용 항목은 하나도 안 버렸다**(위 FAQ 편입표가 그 증거다).
- **판단이 들어간 곳** — 넷

  **ⓐ 목차를 없애고 「갈림 한 문장」으로 바꿨다.** §3은 *"긴 편에서만 남긴다"*고 했고 실측상 명백히 필요한 편은 `/2` 하나였다. 4,936자는 그 선 아래다. 대신 도입 둘째 문단에 *"주가나 시가총액 때문에 걸리신 분은 아래 두 번째 대목에서 …, 재무제표나 공시 때문에 걸리신 분은 세 번째와 네 번째 대목이 각각 자기 자리입니다"*를 넣었다(작법기준 §14-3). **조건을 지켰다 — 갈래 셋에 실제로 그 절이 있다**(h2 #2·#3·#4).

  **ⓑ 요약박스를 없앤 자리를 후킹 문장으로 갈았다.** §3은 *"전부 제거하지 않는다, 편마다 형태를 다시 정한다"*고 했다. `/13`은 **통념을 세우고 뒤집는 글**이라(작법기준 §1 4번째 방식) 수치 박스보다 **첫 문장에서 통념을 세우는 쪽**이 맞다 — *"여섯 글자를 다 읽기도 전에 끝났다는 생각이 먼저 듭니다. 그런데 이 문장은 결과가 아니라 순서가 시작됐다는 통보입니다."*

  **ⓒ 절 제목에서 번호를 뺐다.** `/13`은 원래 「1. ~ 8.」이었다. 통합 뒤 절이 **독자가 믿고 있는 문장**(「벌점 10점은 여러 번 어겨야 쌓이는 게 아닙니다」)으로 달리면 **번호 없이도 순서가 느껴진다**(작법기준 §14-4). 목차를 없앴으므로 번호가 가리킬 곳도 없어졌다.

  **ⓓ `/15`의 「중대·고의 기준 미확인」을 절 끝에 놓았다.** 원문은 *"이 글에서 세부적으로 단정하지 않습니다"*로 자기 글의 무능을 선언하는 쪽이었다(작법기준 §11-4②). **「이 글에서 확인하지 못했습니다」 + 어디에 적혀 있는지**로 바꿔 절 끝에 놓았다 — 중간에 놓으면 변명, 끝에 놓으면 선긋기다(작법기준 §13).

---

## 처분안·실측과 어긋난 것 — **판정하지 않고 적는다**

| # | 처분안/실측이 적은 것 | 오늘 라이브 | 센 명령어 |
|:-:|---|---|---|
| 1 | `/2` 목차 10항 · FAQ Q6 · 요약박스 O · 9,039자 | **셋 다 0개 · 6,766자** | `grep -c '목차\|자주 묻는 질문\|핵심 요약' body2.html` → **0** |
| 2 | *"`/2` §6이 `/9`의 표를 이미 싣는다"* · *"「코스닥 250억」 예시까지 `/9`·`/23`과 같다"* | **`/2`에 표 없음 · `/9`에 250억 없음** | `grep -o '500억원' p2.html\|wc -l` → **0** / `grep -c '250억' p9.html` → **0** (`p23` → 1) |
| 3 | 「실측」 본문 자수 `/9` 3,150 · `/13` 3,766 | **「서사」 명령으로는 2,420 · 2,909** | 위 한 명령. **어느 쪽이 옳은지 판정하지 않는다** — 같은 명령으로 정리본을 세면 `/9` 정리본도 2,420이라 「실측」의 *「라이브=정리본」* 결론은 그대로 성립한다 |
| 4 | *"내부 링크는 21편 중 4편에만 있다(`/2` 5개 …)"* | **`/2`는 7개**(`/5` `/7` `/9` `/10` `/14` `/15` `/23`) · **`/13`도 2개 + 깨진 3개** | `grep -o 'moneyproducer\.co\.kr/[0-9]*' p2.html \| sort -u \| wc -l` → **7** |
| 5 | (어디에도 없음) | **`/13` 3건 · `/14` 1건 · `/15` 2건의 링크가 `href=".../N"`으로 깨져 있다** | `grep -o 'moneyproducer\.co\.kr/N"' p13.html \| wc -l` → **3** |

> **⑤는 묶음3이 `/21`·`/22`에서 찾아낸 것과 같은 고장이다.** 묶음1 대상 3편에서 **6건** 더 나왔다. **`/13` 3건은 닫았고, `/14`·`/15` 3건은 URL 자체가 사라진다.** **남은 편에도 같은 고장이 있는지는 묶음1이 세지 않았다.**

---

## 묶음1이 안 한 것 — 경계

- **`/7`을 한 글자도 안 고쳤다.** 이관_1이 묶음2에 넘어간다
- **`/23`을 한 글자도 안 고쳤다.** 라이브를 읽기만 했다. 이관_2가 묶음2에 넘어간다
- **`/9`·`/14`·`/15`의 본문 파일을 만들지 않았다.** 사라질 URL이다
- **배정 밖의 편을 열지도 고치지도 않았다.** 연 것은 `/2` `/7` `/9` `/13` `/14` `/15` `/23` **7편**이고, `/7`·`/23`은 **이관 2건이 지시한 읽기**다
- **조회 0콜.** 라이브 HTML은 「실측」이 떠 둔 것을 그대로 썼다(`폐기/20260922_실측_라이브/`). **계획 밖 조회 없음**
- **중간물**: `폐기/20260922_묶음1/cut2_병합금지.html` 1건. 프로젝트 안에 원본 HTML을 두지 않았다(D-076 §3-2)
- **자기 글을 스스로 검증하지 않았다.** 위 숫자는 전부 **센 것**이고, 「맞다」는 세지 않았다
