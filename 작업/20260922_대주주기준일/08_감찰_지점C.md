# 08 감찰 — 지점 C (11 데스크 · 12 지면 · 13 사장 도달분)

**2026-09-22 16:4x KST · 「감찰」 · 「진행」이 불렀다(D-047) · 회장 부재**
**「감찰」은 반려하지 않는다. 센 것만 적고 사장에게 올린다. 품질을 말하지 않는다.**

---

## 0. 무엇을 열었나 — 실물만

| 실물 | 무엇을 했나 |
|---|---|
| `07_데스크판정.md` 226행 전건 | 전문 열람 · 병기 명령어 **전건 재실행** |
| `05_원고.md` | 「서사」가 병기한 python 블록 **그대로 실행** |
| `03_기준글_1/2/3.md` | 데스크 인용 **7문자열 축자 대조**(`grep -l -F`) |
| `운영/11_점검표.md` 58~110행 | 7b 6항목 · 11번 7항목 문면 |
| `운영/06_기존글대장.md` 144~200행 | 라이브 제목표 **행번호까지** |
| `04_설계서.md` ↔ `04_설계서_v2_링크개정전.md` | `diff` 전건 |
| `07_원문_실측6G/` 5파일 | `ls` · `wc -c` · 헤더 원문 · `<title>` · 도메인 추출 |
| `00_공정일지.md` §42~§43 (3449~3615행) | 전문 · `[수취]` 전수 grep |
| `01_실측기록.md` · `운영/09_조회표.md` 56행 | 직전 감찰 발견 셋의 처리 상태 |
| `00b_착수계획_실측.md` · `00b_착수계획.md` | 도구표 대조 |
| `운영/감찰보고/` | `ls` |

**보고서를 근거로 적은 줄은 0이다.** 새 조회 **0콜**.

---

## 1. 발견 — 여덟

### ①🔴 「실측」 ②-a 집행 — **`[수취]` 서명이 어디에도 0건이다. 지점 A가 같은 것을 잡았고 「진행」이 「두 번 나오지 않게 했다」고 박아 둔 자리다**

`00_공정일지.md` 3558행이 **「🟢 집행했다(16:3x)」**로 적는다.

```
grep -n '\[수취\]' 00_공정일지.md | grep '실측'   → 0건
grep -c 'OPN0202' 01_실측기록.md                  → 0
ls --time-style=+%H:%M 01_실측기록.md             → 16:27 (②-a 이전 시각)
```

**디스크에는 콜의 실물이 있다.**

| 파일 | 바이트 | 시각 |
|---|---:|---|
| `OPN02020103.html` | **202,625** | 16:38 |
| `OPN02020104.html` | **208,284** | 16:38 |
| `headers_OPN02020103_04_2응답.txt` | 712 | 16:38 |

**콜은 돌았고 원문도 보존됐는데, 받았다는 서명과 기록이 없다.**

**같은 것이 두 번째다.** `00_공정일지.md` **1915행** — 「**지점 A 감찰이 잡은 상시 예외 ⓐ(4번 검사를 `[수취]` 없이 시작)를 「실측」 지시문에 박았다. 같은 것이 두 번 나오지 않게 했다.**」
그리고 **사장 결정 51(3219행)**이 **「`[수취]`는 공정일지에. `[자리명]` 완료 서명은 산출물 파일 말미에」**를 16:2x에 확정했고, **40-3이 「이번 회차부터 모든 호출문에 두 줄을 넣는다」**고 적었다. **②-a는 그 뒤 호출이다.**

**이 검체 전체에서 `[수취]` 「실측」은 공정일지에 0건이다.** 있는 것은 `01_실측기록.md` **540행** 한 건인데, 결정 51이 정한 자리의 반대편이다.

**판단하지 않음** — 「실측」이 아직 돌고 있어 안 적은 것인지, 안 적기로 한 것인지 「감찰」은 모른다.

### ②🔴 점검표 **11-4를 실행하지 않았다.** 그리고 **칠 자리가 공정에 없다**

`운영/11_점검표.md` **80행** 문면:

> | 4 | **링크가 살아 있는가** — 내부·외부 전건 응답 코드 확인 |

`07_데스크판정.md` §3 항목4는 **「조건부 통과」**다. 데스크 자신의 문장 — 「**이번에 응답 코드를 치지 않았다**」 · 「**발행 전에 누군가 4개 + 추가될 `/25`를 쳐야 한다**」.

```
grep -n '조건부' /home/user/webtest/운영/11_점검표.md   → 0건
```

**「조건부 통과」는 점검표에 없는 판정어다.** 점검표 11번 일곱 항목 중 **하나가 실행되지 않은 채 원고가 다음으로 간다.**

그리고 그 일을 받을 자리가 비어 있다.

| | |
|---|---|
| 12번 「지면」 | **안 돈다** — `00_공정일지.md` 3087·3502·3505행, 근거 표기는 **「사장 지시」** |
| 「진행」 처분 | 3605행 — **「콜이 드는 일이라 「진행」이 배정하지 않는다」** |

**12번 「지면」 산출물은 폴더에 0건이다**(`ls | grep -i 지면` → 0). **데스크가 반려한 뒤 지면을 만들지 않은 것은 순서대로다.** 다만 **점검표 12번 항목 전체와 11-4가 함께 사라진다.**

**판단하지 않음** — 발행을 안 하니 안 쳐도 되는지는 사장이 정한다. **「감찰」이 센 것은 「점검표 한 항목이 실행되지 않았고, 실행할 자리가 배정되지 않았다」다.**

### ③ 데스크 기재 행번호 — **`/27`은 169행이다. 「170행」으로 적혔고 「진행」이 그대로 옮겼다**

`07_데스크판정.md` §3 항목4: 「내부 4개 전건이 `06_기존글대장.md` 라이브 제목표에 있고(**161·163·164·170행**)」

```
grep -n '`/19`\|`/21`\|`/22`\|/27`' 운영/06_기존글대장.md
→ 161: /19   163: /21   164: /22   169: /27
sed -n '170p' 운영/06_기존글대장.md   → (빈 줄)
```

**셋은 맞고 하나가 한 줄 어긋난다.** `00_공정일지.md` **3605행**이 같은 「161·163·164·170행」을 **그대로 옮겨 적었다.**

**같은 문서의 다른 행번호는 맞다** — §1-1의 `/25` = **167행**, `grep -n` 재실행 일치.

### ④ 「설계」 ①-a — **지정 두 곳 외에 항목 하나를 신설했다.** 「재량」이라고 표기돼 있다

`diff 04_설계서_v2_링크개정전.md 04_설계서.md` 전건:

| 바뀐 곳 | 지정됐나 |
|---|---|
| 항목 **7** — L5 = `/25` 한 줄 추가 · 제목 「4개」→「5개」 | **지정됨**(데스크 ①-a) |
| 항목 **6-10** — 「4개」→「5개」 + 6-10-a 사유 | **지정됨** |
| 항목 **7-1-a · 7-1-b · 7-2 보강** | 위 두 곳의 근거·불간섭 표기 |
| 항목 **4-5-a 신설(3행)** — 「1회용」 | **지정 밖.** 데스크 §6 **지적**(반려 아님)을 「진행」이 ①-a 호출문에 실었고, 「설계」가 「**되돌림 아님. 「설계」 재량**」으로 표기하고 남겼다 |
| 말미 `[설계]` 서명 | — |

**`05_원고.md`는 한 글자도 안 바뀌었다**(ls 16:27 · diff 대상 아님). 이전판 보존 **`04_설계서_v2_링크개정전.md` 27,007 B** 있다.

**판단하지 않음** — D-071 「지정된 것만 고친다」에 걸리는지는 사장이 정한다. **주체와 사유가 글자로 있다.**

### ⑤ `04b_사전검.md` 부재 — **7b가 안 돌았는데 8번이 열렸다**

```
ls 04b*   → No such file or directory
```

`운영/11_점검표.md` **58행**: 「**`04b_사전검.md`가 없으면 8번(집필)이 열리지 않는다.**」

**「데스크」가 11번 자리에서 6항목을 대신 봤고 전건 통과시켰다.** 데스크·「진행」 둘 다 이미 기록했다(§8-1 · 43-6-2).
**「감찰」이 더한 것은 파일 부재의 재확인 하나다.**

### ⑥ `운영/감찰보고/`에 이 검체 보고 **0건** — **이 보고서도 그 경로 밖에 난다**

```
ls /home/user/webtest/운영/감찰보고/
→ 2026-09-22_20261005_휴장_지점A.md / _지점B.md / _지점C.md
   20260919_조직문서-1차.md / -2차.md
  ("20260922_대주주기준일" 0건)
```

작업 폴더의 `08_감찰_*.md`는 이 파일까지 **5건**이고 전부 규약 경로 밖이다.
**「진행」의 호출문이 산출물을 `작업/…/08_감찰_지점C.md`로 지정했고 「감찰」은 그대로 냈다.**
**「진행」은 「옮기지 않는다 — 남의 산출물이다」로 처분했다**(43-6-3). **「감찰」도 스스로 옮기지 않는다. 경로 규약은 「감찰」이 정하는 것이 아니다.**

### ⑦ 직전 감찰 발견 셋 중 **둘은 닫혔고 하나는 그대로다**

| 직전(ⓑ3) | 지금 |
|---|---|
| **① `RGL03050300` 원문 0건** | 🟢 **닫힘.** `07_원문_실측6G/RGL03050300.html` **218,340 B**(16:26) + 헤더. 조회표 56행에 sha256까지 등재 |
| **② 「27건 전부 메뉴 라벨」** | 🟢 **닫힘.** `운영/09_조회표.md` 56행에 **「26건 중 25건」** 정정문이 들어갔고 「진행」이 같은 명령어로 재실행한 값을 적었다 |
| **③ `01_실측기록.md`에 6-G가 둘** | 🔴 **그대로다.** `grep -n '^#\+ 6-G'` → **1110행 · 1202행**. 되돌림 지정이 갈리는 상태가 유지된다(D-071) |

**덤으로 「실측」이 자기 결함을 하나 더 스스로 신고했다** — `01_실측기록.md` 1355행: G-5의 「58건도 전부 메뉴 라벨」이 실물로는 **36건 중 25 메뉴 라벨 · 7 메뉴 링크 · 2 `<title>` · 2 조직명**. 「실측」 본인 문장 — **「같은 종류의 결함이 같은 문장에서 두 번째다.**」

### ⑧ 사장 보고용 분량 표(§35-2)가 **2회차 값에 멈춰 있다**

`00_공정일지.md` **3020행 표제 — 「분량 — 사장 보고를 갱신한다」**, 표의 값 **서술 4,460 · 합계 5,000**.
**실물은 4,491 / 5,031이다**(아래 §2에서 재현). 새 값은 **43-1에 있다.** 즉 **일지 안에 두 값이 같이 산다.**

**낡은 값이 남은 곳 셋** — `05_원고.md` 집필 메모 **143·145행**(「서사」가 「고치면 지정 밖 행을 건드린다」고 적고 남김) · `00_공정일지.md` **3024~3025행**.
**13번 사장 보고는 아직 나오지 않았다. 나올 때 어느 표를 집는지가 갈린다.**

---

## 2. 재현해서 **일치한** 것 — 「이상 없음」이 아니라 센 값이다

| 대상 | 기재 | 「감찰」이 센 값 |
|---|---|---|
| 내부링크 수 | **4** | **4** — `/27`·`/22`·`/21`·`/19`. 데스크 §1 python 블록 그대로 |
| 블록인용(인물) | **6** | **6** |
| 본문 금지 형태 | `### `0 · `#### `0 · FAQ 0 · 「자주 묻는」0 · `**`0 | **전건 0** |
| 분량 | 서술 4,491 · 표 540 · 합계 5,031 | **4491 / 540 / 5031** — 「서사」 병기 python 블록 원문 그대로 실행. **전건 일치** |
| `/25` 행번호 | 167행 | **167행** |
| 점검표 11-3 | 「최소 다섯」 | **79행 문면 일치** |
| 점검표 11-6 | 「옆줄도 모르는가」 | **82행 문면 일치** |
| 점검표 11번 항목 수 | 7 | **7** |
| 라이브 편수 | 23편 | **23** — 대장 146~169행 24줄 중 취소선 `/26` 1줄 제외 |
| §5-3 「첫 250자 안」 | 통과 | **216~250자.** 병기 명령어가 없던 숫자인데 **재현된다** |
| **데스크가 기준 글을 열었는가** | 「3편 원문 전건」 | **7문자열 전건 실재.** 기준글1 1 · 기준글2 2 · 기준글3 3 · 제목 3편 각 1. **요약본이 아니라 실물에서 뽑은 문자열이다** |
| 실측 ②-a 원문 보존 | — | **2파일 410,909 B + 헤더 712 B.** `<title>` 「장내청산결제 \| 청산결제개념」 계열. **결정 49 이후 규약대로 보존됐다** |
| ②-a 도메인 | `open.krx`·`info.krx`(결정 52) | **`open.krx.co.kr` 경로.** `00b_착수계획_실측.md` **10행 도구표 안**이다. **계획 밖 도메인 0건** |
| 12번 산출물 | 안 돈다 | **폴더에 0건. 반려 뒤 지면을 만들지 않았다** |
| 설계 이전판 | 보존 | **`04_설계서_v2_링크개정전.md` 27,007 B 실재** |

---

## 3. 「감찰」이 **못 본** 것

| # | 못 본 것 | 왜 |
|---|---|---|
| ① | **「데스크」의 「새 조회 0콜」** | **부재는 기록으로 증명되지 않는다.** 콜 로그가 없다 |
| ② | **②-a 2콜의 요청 URL 전문** | `headers_*.txt`에 응답 헤더만 있고 **요청 URL이 없다.** 도메인은 본문 내 링크로 유추했다 — **유추다** |
| ③ | **「설계」·「데스크」가 무엇을 안 열었는가** | 자기 신고뿐이다 |
| ④ | **13번 사장 보고** | **아직 나오지 않았다.** §43-5가 사장에게 둘을 올린 상태에서 멈춰 있다 |
| ⑤ | **1·1b·4·7·8·10** | 회장 승인 지점 셋 규칙대로 보지 않았다 |

---

## 4. → 사장 확인 바람 — **셋을 먼저**

| 순 | 「감찰」이 센 것 | 사장이 물을 자리 |
|---|---|---|
| **1** | **②-a가 「집행했다」로 일지에 적혔는데 `[수취]` 0건 · 기록 0건.** 지점 A가 잡아 1915행에 「두 번 안 나오게 했다」고 박은 그 자리이고, **결정 51 확정 뒤의 호출이다** | **「실측」** — 받았다는 서명을 어디에 두었는지. **「진행」** — 집행으로 센 근거가 무엇인지 |
| **2** | **점검표 11-4(링크 응답 코드)가 실행되지 않았다.** 「조건부 통과」는 점검표에 없는 판정어다(`grep '조건부'` → 0). **12번이 안 도니 칠 자리가 배정되지 않았다** | **「데스크」** — 치지 않고 통과로 적은 근거. **사장** — 그 콜을 누구에게 붙일지 |
| **3** | **`/27`은 169행인데 「170행」으로 데스크가 적고 「진행」이 대조 없이 옮겼다** | **「진행」** — 옮기기 전에 열어 보았는지 |

**넷째부터** — ④ 「설계」 지정 밖 항목 1개 신설(재량 표기 있음) · ⑤ `04b` 부재 재확인 · ⑥ 감찰보고 경로 · ⑦ 6-G 중복 미처리 · ⑧ §35-2 낡은 분량 표.

**②와 ⑤는 같은 모양이다** — **점검표에 글자로 있는 항목이 실행되지 않은 채 공정이 지나갔다.** 7b는 통째로, 11-4는 한 항목이다. **데스크가 7b를 대신 봐서 막지는 않았으나, 11-4는 대신 본 자리가 없다.**

---

```
[감찰] 2026-09-22 16:4x KST · 「감찰」 — 지점 C. 「진행」이 불렀다(D-047). 새 조회 0콜 · 돈 0.

  연 것    실물 11종. `07_데스크판정.md` 226행 전건 · 기준글 3편 · 점검표 · 대장 ·
           설계서 diff · `07_원문_실측6G/` 5파일 · 공정일지 §42~§43.
           보고서만 읽고 적은 줄 0. `운영/07_결정로그.md` 열지 않았다(D-070).

  센 법    데스크·「서사」·「설계」가 병기한 명령어를 그대로 재실행했다.
           python(내부링크·분량) · grep -l -F(기준글 축자) · grep -n(행번호) ·
           diff(설계 이전판) · wc -c · ls --time-style · find.

  발견     8건. 그중 ㉠ 상시 예외 「[수취] 없는 넘김」 1건 — **지점 A와 같은 자리, 같은 모양** ·
           점검표 미실행 2건(11-4 · 7b) · 행번호 오기 1건이 사장 보고 경로로 전파됨 ·
           직전 감찰 발견 3건 중 2건 닫힘 · 1건(6-G 중복) 그대로.
           ㉡ 계획 밖 도메인 조회 **0건** — ②-a는 도구표 안이다.

  일치     14종 전건 재현. 특히 **데스크가 기준 글 3편을 실제로 열었다** —
           인용 7문자열이 세 파일에 축자 실재한다. 분량 4,491/540/5,031도 전건 일치.
           **반려 뒤 12번 지면을 만들지 않았다 — 순서를 어기지 않았다.**

  안 한 것 반려하지 않았다. 판정하지 않았다. 품질을 말하지 않았다. 이유를 추측하지 않았다.
           `00_공정일지.md`에 쓰지 않았다. 남의 산출물을 옮기지 않았다.
```
