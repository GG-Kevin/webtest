# ⑥ 지면 (12번) — /10 보강

2026-09-21 · 「데스크」 통과분만 만들었다 · 외부 접속 0회(폰트는 스크래치패드에 있던 Pretendard, OFL 1.1)
산출 — `06_썸네일.png`(1200×630 · 45KB · md5 `16402c3cc19f943c9bb74fc7d8130172`) · `09_발행본.html` · `09_미리보기.html`

> **왜 이 형태인가 (한 줄)** — 이 글의 척추가 표이므로 **썸네일도 표의 한 행으로 짰다.** 공시 문구가 행이고, 368/1,124가 그 행의 길이다.

| | 한 것 | 왜 |
|---|---|---|
| **문구** | 이미지 안 글자는 주·부 두 줄이 전부. 주문구는 줄바꿈만 넣고 **글자 변경 0** — 생성 스크립트가 `L1+" "+L2 == 원문`을 assert로 걸고 아니면 안 그린다 | 「등」·쉼표가 줄면 이 썸네일의 존재 이유가 사라진다 |
| **규격** | 1200×630 | 22장은 800×800이라 오픈그래프에서 잘렸다 |
| 22장 ① 남색 배경 | 배경 `#E7ECE8` 연녹회색 | 22/22가 `#101E30`을 화면의 81~89%로 썼다 |
| 22장 ② 노란 배지 | **배지 없음.** 글자를 두 줄 외에 하나도 얹지 않았다 | 주인공을 밀어낼 것을 안 만든다 |
| 22장 ③ 흰 2줄 제목 | 먹빛 `#10201A` · **좌측정렬**(22장은 가운데) | 명도차 약 14:1 |
| 22장 ④ 노란 키워드 | 부문구 **전체가 한 색** `#2C3E36`. 낱말 강조 0 | 손톱 크기에서 색 하나 차이는 안 보인다 |
| 직전 확정판(거래정지)과 | 저건 **상하 50:50 명암 반전 판 + 가운데정렬**. 이건 단색 바탕 + 좌측정렬 + 가로 괘선 | 배경 처리·배치·색 계열 셋 다 안 겹친다 |
| 직전 폐기판과 | 저건 **크림 바탕 + 좌측 빨강 세로바 + 작은 회색 부문구**. **내가 처음 잡은 안이 정확히 그것이라 실물을 열어보고 버렸다** | 글로 피했으면 또 만들 뻔했다 |
| **막대** | 368/1,124 = **32.7% 길이**. 본문 11·37행 숫자 그대로 | 본문에 없는 것을 이미지로 약속하지 않았다 |
| **축소 검증** | 420 / 300 / 180 / **120px**로 줄여 눈으로 봤다 | 120px에서 주문구 두 줄 읽힌다. **부문구는 300px 아래에서 죽는다 — 알고 둔 것이다** |

## 발행본 HTML — 넷만

- **본문은 `05_원고.md` 1~115행**(`## [수취]` 앞까지). 서명·메모 **0줄**. 태그 제거 후 원고와 **3,203자 대 3,203자 완전 일치** — 한 글자도 더하거나 빼지 않았다.
- **표가 모바일에서 안 깨진다.** 560px 아래에서 표가 **행 단위 카드로 접힌다**(`thead` 숨기고 각 칸에 열 이름을 `data-l`로 붙임). 열 이름은 원고의 표 머리 그대로라 **내가 지어낸 글자는 없다.** 가로 스크롤을 안 만든 이유 — 이 글은 표를 못 읽으면 아무것도 아니다.
- **인라인 스타일을 베끼지 않았다.** 22편이 쓰던 `style="..."` 한 틀 대신 `.mp-post` 아래로만 먹는 `<style>` 한 덩어리로 짰다. 스킨과 충돌하지 않고, 다음 글에서 고칠 자리가 한 군데다.
- **내부링크 1개**(`/5`, 「관리종목 지정되면 어떻게 되나」) · **면책 1회** · h2 6 · h3 3 · 표 3 · 인용 7. 자동 계수로 확인했다.

## 안 한 것

- **문구·제목·본문 무접촉.** 「포착」이 준 두 줄과 원고를 고칠 이유가 없었다.
- **거부권 안 썼다.** 주문구 24자가 1200px 폭에서 두 줄 · 120px에서도 읽혀서 되돌릴 근거가 없었다.
